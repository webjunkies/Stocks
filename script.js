



























//variables
/*var amount = 0, amountFactor = 1;

//functions
function click(){
    document.getElementById('amount-display').innerHTML = amount;
    document.getElementById('add-amount').innerHTML = amountFactor + 1 + " cost: " + ((amountFactor + 1) + (amountFactor + 1));
};

function add(){
    amount = amount + amountFactor;
    document.getElementById('amount-display').innerHTML = amount;
    document.getElementById('add-amount').innerHTML = amountFactor + 1 + " cost: " + ((amountFactor + 1) + (amountFactor + 1));
};

function addAmount(){
    if(amount >= (amountFactor + 1) + (amountFactor + 1)){
        amount -= (amountFactor + 1) + (amountFactor + 1);
        amountFactor = amountFactor + 1;
    }
    document.getElementById('amount-display').innerHTML = amount;
    document.getElementById('add-amount').innerHTML = amountFactor + 1 + " cost: " + ((amountFactor + 1) + (amountFactor + 1));
};*/