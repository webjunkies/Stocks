function numbers(startAmount, startFactor)
{
    this.startAmount = startAmount;
    this.startFactor = startFactor;
    this.amount = 0;
    this.netWorth = 0;
    this.factor = 0;
    
    this.setup = function(context){}
    
    this.update = function(context){}
    
    this.render = function(context){}
}